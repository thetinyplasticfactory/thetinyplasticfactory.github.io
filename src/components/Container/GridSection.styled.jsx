import styled, { css } from 'styled-components';

import { Container } from './Container.styled';

export const GridSection = styled(Container)`
  padding: 3em;
  flex-direction: column;
  align-items: flex-start;
  width: auto;
`;
